7-1介面的基本概念
介面(Interface)是一種定義抽象方法的抽象類別，用於定義抽象方法的抽抽象類型，用於定義：
	1.一個類別應該實現的方法規範
	2.但並不提供這些方法的實作內容
	介面緊定義了方法的簽名(方法名稱、返回類型、參數列表)，而不包含方法的具體實現
7-1-1
介面可以用來整合類別的共通行為
	不同的類別需要共享某些行為或功能時
	介面提供了一個非常有用的機制
	其允許類別定義一組方法，而這些方法可以被實現為具體的功能
介面允許類別根據需要實現這些方法
	不需要在類別之間建立直接的繼承關係
Java程式語言中的介面是一個抽象型別(Abstract Type)
	可用來要求類別(Class)必須實作指定的方法
	使不同類別的物件得以使用相同的介面進行溝通
介面通常以interface來宣告
	他僅能包含方法簽名(Methood Signature)
	以及常數宣告(變數宣告包含了static及final)
	一個介面僅會定義方法會包含方法的實作
介面定義不同類別之間的一致行為
	也就是一些共同的屬性(常數)或抽象方法
	如同一些規範
一個類別可以實現(Implement)多個介面
	如此就能同時達到繼承多個規範的要求
	就像是實現多長繼承的效果
介面亦可稱為介面類別，通常稱之為介面
	其編譯後會產生 介面名稱.class的檔案
介面之宣告並非使用class而是改用interface，且不需修飾詞
	介面中的方法只有宣告，沒有建構子
	介面的成員變數在宣告時就必須指定初始值
	且於繼承後無法改變(其欄位都必須宣告為具有初始值的靜態常數)
介面不可以用以產生物件實體，只能被實作
若要在類別中使用介面，則需在類別名稱後加上關鍵"implements"，隨後加上介面名稱
隨後加上介面名稱
	舉例來說，假設我們有不同物種的動物，他們要發出聲音的功能
	可以定義一個介面，其中包含發生方法
	不同動物類別可以實現這個介面，他們都具有了發聲音的能力
	狗類別會發出聲音，然而透過介面，貓類別不必為了發出聲音去繼承狗的類別特色，減少不必要的繼承關係
```
package javatest1;

interface Soundable{

void makeSound();

}

class Dog implements Soundable{

_@Override_

public void makeSound() {

System.**_out_**.println("狗發出聲音");

}

}

class Cat implements Soundable{

_@Override_

public void makeSound() {

System.**_out_**.println("貓發出聲音");

}

}

class Dogando1 {

public static void main(String[]args) {

Soundable dog =new Dog();

Soundable cat =new Cat();

dog.makeSound();

cat.makeSound();

}

}
```
7-1-2為何使用介面
介面是一個類別的藍圖，它包含靜態常數和抽象方法
在java介面中，只能包含抽象方法，而不能包含完整實現的方法，所以介面無法被實例化。
因為介面可以俵是is-a的關係，所以透過介面可以實現抽象畫、多重繼承、以及可以用來實現鬆散耦合
鬆散耦合是指程式中的模組或元件之間盡可能減少彼此之間的相依性
這種設計原則的增加程式的靈活性和可維護性
使得程式中的不同的程式碼可以獨立地變更
不會對其他部分程式造成影響
鬆散耦合的設計通常可以使用介面或抽象類別來定義模組之間的互動關係
	這樣可以隱藏實現細節，只暴露必要的方法和屬性
	減少了模組之間的相依性，如此在系統擴充和維持時會具有靈活性
7-1-3介面與類別的關係
介面可以繼承其他介面
	這種繼承關係允許一個介面繼承另一個或多個介面
當一個介面繼承了其他介面時
	他就繼承了被繼承介面的所有方法規範
	 包括方法的名稱、返回值類型和參數等
介面的使用有助於實現程式碼的模組化和靈活性
	使得不同的類可以實現相同的介面
	提高了程式碼的可擴展性和可維護性
	
物件導向設計(OOP)著重於將現實世界中的事物抽象畫為程式世界中的物件。
著重於四大特性，分別是封裝、繼承、多型、抽象。

封裝
是一種保護物件內部狀態的機制
同時提供一個公開的介面供外部使用
	1.在封裝中，物件的內務狀態被隱藏
	2.透過物件的方法(getter and setter)才能存取和修改內部狀態
不僅確保了數據的安全性，還隱藏了現實的細節，使得程式更容易維護和修改

所以這樣設計方法提供了以下幾個重要優勢：
	資料的安全性：
		封裝可以保護類別的屬性，防止外部直接存取和修改
		只定義了公開(public)的getter和setter方法，外部才能存取和修改
		確保數據的合法性，避免不當的操作導致數據混亂
	實現細節的隱藏：
		封裝隱藏了類別的內部實現細節
		外部使用者只需要知道如何使用公開的方法，不需要知道實現細節
	程式碼的重複使用：
		封裝使得類別的內部實現與外部介面分離
		如果類別的內部設計需要改變，只需要確保外部介面保持不變，就不會影響到外部程式碼
	6-1-1封裝
```
public class BankAccount {
private double balance;//餘額，使用private關鍵字封裝
//提供公開的方法存取和修改餘額
public void setBalance(double balance) {
//檢查輸入的餘額是否合法，這裡可以加入各種業務邏輯的檢查。
	if(balance>=0) {
	this.balance = balance;
	}else {
System.**_out_**.println("無效餘額！");
	}
}
public double getBalance() {
return balance;
	}
}	
```
	繼承：
		允許一個類別(子類別，Subclass)繼承另一個類別(父類別，Superclass)的屬性和方法。
		表示該子類別可以使用父類別的所有共用(public)數性和方法，並且可以在此基礎上擴展(Extends)或修改他們。
		子類別可以透過關鍵字extends來使用父類別的屬性和方法，同時還可以改寫(Override)父類別的方法來適應特別需求。
		6-1-2繼承
```
	package hw0528;

  

public class NestedTryCatch {

    public static void main(String[] args) {

        try {

            _throwException_();

        } catch (Exception exception) {

            System.**_out_**.println("在main method 捕獲 Exception No.2");

        }

  

        _doesNotThrowException_();

    }

  

    public static void throwException() throws Exception {

        try {

            System.**_out_**.println("throwException Method 拋出 Exception No.1");

            throw new Exception();

        } catch (Exception exception) {

            System.**_out_**.println("method(throwException) 捕獲 Exception No.1");

            System.**_out_**.println("method(throwException) 拋出 Exception No.2");

            throw exception;

        } finally {

            System.**_out_**.println("執行 throwException() 內部之 Finally!");

        }

    }

  

    public static void doesNotThrowException() {

        try {

            System.**_out_**.println("Method doesNotThrowException 沒有捕獲到 Exceptions!!");

        } catch (Exception exception) {

            System.**_out_**.println(exception);

        } finally {

            System.**_out_**.println("執行 doesNotThrowException 內部之 Finally!!");

        }

  

        System.**_out_**.println("完成 doesNotThrowException 之呼叫!!");

    }

}
```
	多型：
		是一種能夠根據當前程式情境自動選擇適當行爲的特性
		在OOP5中，如果某個類別的物件具有父類別或介面的型別，那麼他可以接受那個父類別或介面的型別，使得相同的方法或屬性可以被不同的物件類別呼叫
		多型性提高了程式碼的靈活性，以及可擴展性。
		多型概念，可以定義一個通用的介面(General interface)或父類別，讓不同的子類別實現這個介面或繼承這個父類別。
		使用通用的介面或父類別類行來操作著些不同子類別的物件。
		允許不同類別的物件被視為同一種類型的物件，這樣就可以使用相同的方法名稱來操作這些不同類型的物件。
		多型的實現依賴於動態綁定(Dynamic Binding)的機制，在執行期間結定所需呼叫的實際物件方法。
		在多型的概念下，方法的名稱可以被不同的類別共用，也就是方法的(Overriding)重載。
		子類別也可以改寫(Overriding)父類別的方法，這也是多型的體現之一。
		6-1-3多型
```
		package javatest1;

  

class Furits{

public void tast() {

System.**_out_**.println("這是一種普通的水果");

}

}

  

class Apples extends Furits{

_@Override_

public void tast() {

System.**_out_**.println("這是一個蘋果甜甜的");

}

}

  

class Oranges extends Furits{

_@Override_

public void tast() {

System.**_out_**.println("這是一個程子酸酸的");

}

}

  

class Grapes extends Furits{

_@Override_

public void tast() {

System.**_out_**.println("這是一串葡萄好吃的");

}

}

public class FruitsPolyTest {

public static void main(String[]args) {

Furits myFruit = new Apples();

myFruit.tast();

myFruit = new Oranges();

myFruit.tast();

myFruit = new Grapes();

myFruit.tast();

}

}		
```
	抽象
		將類別的共同特性提取出來，形成抽象類別或介面。
		隱藏其具體實現細節，僅顯示功能特性。
	抽象類別
		一個未被完整實例化的類別
		定義了一個類別的基本結構和方法，但是沒有具體的實現
		抽象類別是一種特殊的類別
			他無法被直接實例化。
				抽象類別不能被直接實例化為物件，因為它包含抽象方法。抽象方法是指尚未被具體實現的方法。
				必須包含一個以上的抽象方法，抽象類別可以包含抽象方法，抽象方法是一個沒有實作內容的方法，只有方法的的簽名(Methood Signature)，用abstract關鍵字標記。這些方法尚未被具體實現，再藉由子類別來實現這些抽象方法。
				可以包含具體方法，抽象類別也可以包含具體的方法(具備完整功能的實作方法)，這些方法可以被繼承的子類別直接使用，無需重新實現。
			即無法使用關鍵字new創建該類別的物件。
			主要用於定義具有共同特徵和行為的類別。
			為子類別提供一個統一的模板。
	為典型的抽象類別之樣式：
		抽象類別名稱
			定義了一個抽象類別的名稱，使用abstract關鍵字來聲明他是一個抽象類別。
		屬性(可有可無)：
			抽象類別可以包含屬性，這些屬性可以是一般的屬性，也可以是抽象類別的特有屬性。
		抽象方法：
			抽象類別中包含至少一個抽象方法，使用abstract關鍵字來聲明他是一個抽象方法。抽象方法本身在抽象類別中並沒有被實作，只有方法的聲明，具體的實作則由子類別來完成。
		一般方法：
			抽象類別中可以包含一般方法，這些方法本身在抽象類別中已被實作，一般方法的區塊內提供了具體的程式碼。這些方法可以被子類別直接繼承或改寫。
			典型的類別之樣式：
```
	abstract class 抽象類別名稱{
	//抽象類別的屬性
	類型 屬性名稱;
	//抽象方法
	public abstract 返回類型 方法名稱(參數列表);
	//一般方法
	Public 返回類型 方法名稱(參數列表){
		//方法的實作
	}
	}		
			
```
	介面
		一個僅包含抽象方法(Abstract Methood)和常數的類別
		他定義了一個物件的功能契約
		在抽象概念下，只關心對特定問題建立模板，而忽略其他不必要的細節
	6-1-4 抽象
```
	package javatest1;

//抽象類別

abstract class Fruit1{

public abstract void taste();

}

//具體類別Apple1繼承自Furit1

class Apple1 extends Fruit1{

_@Override_

public void taste() {

System.**_out_**.println("這是一個蘋果甜甜的");

}

}

class Orange1 extends Fruit1{

_@Override_

public void taste() {

System.**_out_**.println("這是一個橘子甜甜的");

}

}

  

public class FruitAbsTest {

public static void main(String[]args) {

Fruit1 myApple = new Apple1();

myApple.taste();

Fruit1 myOrange = new Orange1();

myOrange.taste();

}

}
	
```
Fruit1是一個抽象類別(Abstract Class)，他定義了所有水果都必須實現的taste()方法
Apple1 和Orange1是具體的水果類別，他們繼承了Fruit1並實現了taste()方法
這樣的設計使得我們可以專注於水果和共同特徵(味道，taste())，並且允許具體化水果類別來實現這一特徵。

```
package javatest1;

abstract class Fruit2{

protected String name;

public Fruit2(String name) {

this.name = name;

}

//抽象方法，子類別必須實作

public abstract void describeTaste();

//具體方法

public void wash() {

System.**_out_**.println("洗過了");

}

}

class Apple2 extends Fruit2{

public Apple2(String name) {

super(name);

}

_@Override_

public void describeTaste() {

System.**_out_**.println(name+"的味道是甜脆的");

}

}

  

public class FruitAbsDemo {

public static void main(String[]args) {

Fruit2 myApple = new Apple2("蘋果");

myApple.describeTaste();

myApple.wash();
}
}
```

抽象類別(Abstract Class)是一種在java中的類別，他可以聲明方法名稱但不提供具體實作
只要具有抽象方法的類別就被稱為抽象類別
抽象類別無法被用來實例化物件
只能被繼承並在子類別中實作未完成的抽象方法
6-3-1抽象類別的特性
使用關鍵字"abstract"來定義抽象類別(Abstract Class)和抽象方法(Abstract Method)
任何類別如果含有抽象方法，該類別必須被定義為抽象類別
子類別必須實作其父類別中的所有抽象方法，否則子類別也必須聲明為抽象類別
抽象類別為其子類別提供了共用的方法和變數，這樣可以保持程式碼的醫致性
抽象類別的使用可以降低程式的耦合度(Coupling)，增加程式的擴展性
它常被用來設計介面(Interface)或者一個類別層次結構的基礎

| 內容  | 定義                         | 例子                                   |
| --- | -------------------------- | ------------------------------------ |
| 耦合度 | 兩個模組(類別、方法、程式區塊)之間互相依賴的程度。 | 兩個類別之間共享大量的實力資料、方法或變數。               |
| 低耦合 | 模組之間只需要少量、明確的資訊互動;能夠獨立修改。  | Book與StoryBook只共用Book.getTopic()等方法。 |
| 高耦合 | 模組之間互相依賴、變更影響範圍大。          | 大量public欄位、共用靜態變數、方法等引用。             |
程式內聚(Ohishoin)概念是什麼？？
	內聚(Ohishoin)指的是一個類別、模組或函式內部各個部分之間功能關聯度的高地。
		高內聚：同一個模組內的所有元素(屬性、方法、子模組等)都在為同一個核心功能服務，彼此互相依賴且互補。
		低內聚：模組內的元素彼此關聯不大，往往執行多種不相關的工作，導致維護成本上升、重用困難。
在實際開發中，務必遵循
	單一職責原則
	低耦合＋高內聚的設計哲學
6-3-1抽象類別的特性
	1.多形(Polymorphism)提供了程式架構的彈性和可維護性，多型常透過抽象類別、介面、方法改寫，以及建構子過載來實現。
	2.抽象類別定義了一個共同的操作介面，而具體的子類別實現了這些方法當程式碼依賴於抽象類別或介面時，可以輕鬆地在不修改現有程式碼的情況下，引入新的子類別，從而提高了程式的擴展性和可讀性。
	3.抽象類別、方法改寫、以及建構子過載概念促使了多型性的實現，使得程式碼更有彈性，能夠適應不同類型的物件。
6-3-3採用繼承類別的設計
```
package javatest1;

class FruitInheritance{

private String color;

private double weight;

public FruitInheritance(String color,double weight ) {

this.color= color;

this.weight= weight;

}

public String getColor() {

return color;

}

public double getweight() {

return weight;

}

public String getTaste() {

return "unknow";

}

public String getTexture() {

return "unknow";

}

}

class AppleInheritance extends FruitInheritance{

public AppleInheritance(String color,double weight) {

super(color,weight);

}

_@Override_

public String getTaste() {

return"甜";

}

_@Override_

public String getTexture() {

return"脆";

}

}

class OrangeInheritance extends FruitInheritance{

public OrangeInheritance(String color,double weight) {

super(color,weight);

}

_@Override_

public String getTaste() {

return "酸甜";

}

_@Override_

public String getTexture() {

return"多汁";

}

}

  

public class FruitInheritanceDemo {

public static void main(String[]args) {

FruitInheritance apple = new AppleInheritance("紅色",0.2);

FruitInheritance orange = new OrangeInheritance("橘色",0.3);

System.**_out_**.println("蘋果顏色"+apple.getColor());

System.**_out_**.println("蘋果重量"+apple.getweight()+"公斤");

System.**_out_**.println("蘋果味道"+apple.getTaste());

System.**_out_**.println("蘋果質地"+apple.getTexture());

System.**_out_**.println("橘子顏色"+orange.getColor());

System.**_out_**.println("橘子重量"+orange.getweight()+"公斤");

System.**_out_**.println("橘子味道"+orange.getTaste());

System.**_out_**.println("橘子質地"+orange.getTexture());

}
```
		上述程式class FruitAbs類別變成了抽象類別，它包含了兩個抽像方法get